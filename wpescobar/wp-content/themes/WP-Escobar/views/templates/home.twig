{% extends "layouts/base.twig" %}

{% block layout 'home' %}

{% block content %}

<div class="home_container">

    <h5>{{post.description}}</h5>
    <h2>{{post.title}}</h2>

    <div class="btns">

        <a href="{{post.get_field('url_bouton_1')}}" class="home_btn">{{post.get_field('texte_bouton_1')}}</a>
        <a href="{{post.get_field('url_bouton_2')}}" class="home_btn">{{post.get_field('texte_bouton_2')}}</a>

    </div>

</div>

{% endblock %}